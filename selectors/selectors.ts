export const Selectors = {
    // Navigation
    homeLink: "//a[contains(text(), 'Book Cart')]",
    
    // Common elements
    loadingSpinner: ".loading-spinner",
    errorMessage: ".error-message",
    
    // Add more selectors as needed
};